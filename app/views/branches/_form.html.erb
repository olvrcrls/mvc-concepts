<%= simple_form_for @branch do |form| %>
	<%= form.input :name, label: "Name" %>
	<%= form.input :city, label: "City" %>
	<%= form.button :submit, { class: "btn btn-md btn-success" } %>

	<%= link_to "Return", branches_path, { class: "btn btn-md btn-default" } %>
<% end %>