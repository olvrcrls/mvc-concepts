<%= simple_form_for @brand do |form| %>
	<%= form.input :name, label: "Name" %>
	<%= form.button :submit, { class: "btn btn-md btn-success" } %>

	<%= link_to "Return", brands_path, { class: "btn btn-md btn-default" } %>
<% end %>